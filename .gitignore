.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.mypy_cache/
node_modules/
dist/
build/
.env
.env.integration
.env.local
.handoff/
.DS_Store
*.pyc
*.egg-info/
coverage/
!tests/integration/acme/coverage/
!tests/integration/acme/coverage/**
# But still exclude compiled cache inside the included dir
tests/integration/acme/coverage/__pycache__/
playwright-report/
test-results/
out/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.sqlite-journal
# Runtime data dir: app sqlite + compiled wiki + chroma local files
backend/data/
tests/integration/results/connector-results.json
tests/integration/acme/seed/acme_ids.json
coverage-*.json
coverage-live-*.json
chat-*.json
compile-retrieval.json
agents.json
artifacts/
backend/app/static/
data/wiki/

# Secrets — never commit
api_tokens.txt
*_tokens.txt
*.pem
*.key
secrets/

# Internal-only: code review dumps and team handoff docs (not for OSS distribution)
.reviews/
docs/internal/
goal.md

# Claude Code local config and installed skills
.claude/

# BigQuery emulator seed data — regenerated by tests/integration/seed/bigquery/load.py
tests/integration/seed/bigquery/data/
data/

# Stale internal test runbook (v0.3-era, superseded by docs/TESTER_GUIDE.md)
tests/integration/TEST_RUNBOOK.md

# Branch-local working notes — kept on disk, never committed.
docs/BRANCH_ISSUES_STATUS.md

# Internal manual testing guide for QA — kept on disk, never committed.
docs/EVALS_TESTING_GUIDE.md

# Phase test files (Evals + Langfuse intelligence layer) — kept on disk
# for local QA, not committed. Features ship without their pytest scaffolding.
backend/tests/test_phase*.py
